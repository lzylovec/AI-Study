# AI Study（AI 智学通）项目说明文档（答辩/提问版）

本文档面向“代码小白”，用于在老师提问时，能把项目讲清楚：做什么、怎么做、用到哪些技术、核心流程是什么、数据表怎么设计、如何演示。

---

## 1. 项目一句话介绍

AI Study（AI 智学通）是一个基于 SSM（Spring + Spring MVC + MyBatis）的智能学习辅助 Web 应用：支持用户上传语音/文档资料，把内容转成可编辑笔记，并利用 NLP 抽取关键词构建知识图谱，用可视化方式帮助复习与梳理知识点。

---

## 2. 我做这个项目解决了什么问题

- 学习资料来源多：上课录音、PDF/Word、零散笔记难管理。
- 资料不结构化：复习时找重点难、知识点之间关系不直观。
- 传统笔记效率低：听完课再整理耗时。

本项目的思路是：把“语音/文档 → 可编辑文字 → 关键词 → 知识图谱”串起来，形成学习闭环。

---

## 3. 技术栈与为什么这么选

### 3.1 后端（SSM）

- Spring（容器/依赖注入）：把 Service、Mapper、Controller 组织起来，便于解耦。
- Spring MVC（Web 层）：提供页面路由和 API 接口。
- MyBatis（持久层）：用 XML 写 SQL，直观、适合教学。
- MySQL：存用户、笔记、资料、知识图谱结构等。
- Maven：依赖管理、打包成 `war` 部署到 Tomcat。

证据：`pom.xml:1` 定义 `packaging` 为 `war`，并引入 Spring/MyBatis/MySQL 等依赖。

### 3.2 前端

- Thymeleaf：服务端模板渲染页面，简单易懂（适合课程项目）。
- Bootstrap 5：快速做出好看的界面。
- D3.js：绘制知识图谱力导向图，支持拖拽/缩放。

证据：页面模板在 `src/main/resources/templates/`，静态 JS 在 `src/main/resources/static/js/`。

### 3.3 AI / NLP 能力

- HanLP：中文关键词抽取（用于知识图谱节点）。`NLPService.keywords` 调用 `HanLP.extractKeyword`（`src/main/java/com/study/kgraph/service/NLPService.java:9`）。
- 共现关系：以“同一句/同一段落出现”为边（关系强弱按次数计数）。`NLPService.cooccurrence`（`src/main/java/com/study/kgraph/service/NLPService.java:15`）。
- 文档解析：Apache Tika 从 PDF/Word 抽取文本（`src/main/java/com/study/kgraph/service/TikaService.java:11`）。
- 语音识别：集成阿里云 ASR，短语音走同步识别（`src/main/java/com/study/kgraph/service/SpeechServiceAliyun.java:24`）。

---

## 4. 项目整体架构（从浏览器到数据库）

可以用一句话回答：

浏览器请求页面/接口 → Spring MVC Controller 接收 → 调用 Service 做业务 → MyBatis Mapper 执行 SQL → MySQL 存取数据 → 返回 JSON 或渲染页面。

对应配置：

- Spring MVC 扫描 Controller：`src/main/resources/spring/spring-mvc.xml:12`
- Spring 扫描 Service/Mapper：`src/main/resources/spring/applicationContext.xml:18`
- MyBatis SQL 映射：`mybatis.mapperLocations=classpath:mapper/*.xml`（`src/main/resources/application.properties:6`）
- 入口 Servlet：`DispatcherServlet` 映射到 `/`（`src/main/webapp/WEB-INF/web.xml:37`）

---

## 5. 核心功能与流程讲解（老师最爱问）

### 5.1 用户注册/登录与会话

- 注册：`/api/auth/register`（`src/main/java/com/study/kgraph/controller/AuthController.java:19`）
  - 存储 `username` + `password_hash`。
- 登录：`/api/auth/login`（`src/main/java/com/study/kgraph/controller/AuthController.java:31`）
  - 校验后把 `userId` 放进 `HttpSession`：`session.setAttribute("userId", u.getId())`（同文件 `:39`）。

拦截未登录访问：

- `LoginInterceptor` 在请求前检查 `session.userId`（`src/main/java/com/study/kgraph/interceptor/LoginInterceptor.java:14`）。
- 对 `/api/**` 返回 401 JSON（便于前端识别登录失效），对普通页面重定向到登录页（同文件 `:23-36`）。

老师追问“为什么用 Session”：

- 课程项目里 Session 简单可用，后端不需要自己管理 token。

### 5.2 页面路由（访问哪些页面）

`PageController` 负责把 URL 映射到 Thymeleaf 模板：

- `/` → `login.html`（`src/main/java/com/study/kgraph/controller/PageController.java:8`）
- `/dashboard` → `dashboard.html`（同文件 `:13`）
- `/notes` → `notes.html`（同文件 `:18`）
- `/graph` → `graph.html`（同文件 `:23`）

### 5.3 语音转写生成笔记（短音频 + 长音频）

入口在 `NoteController`：

- `/api/notes/upload-audio` 上传音频并识别（`src/main/java/com/study/kgraph/controller/NoteController.java:108`）。
  - 如果是 mp3：会走“自动长语音流程”，先上传 OSS，再提交异步转写任务（同文件 `:120-156`）。
  - 非 mp3：直接调用 `SpeechService.transcribe` 同步识别（同文件 `:126`）。

短语音识别实现：

- `SpeechServiceAliyun.transcribe` 使用 HTTP 调阿里云 ASR（`src/main/java/com/study/kgraph/service/SpeechServiceAliyun.java:24`）。
- `AliyunTokenService.getToken` 会缓存 token，避免每次都重新申请（`src/main/java/com/study/kgraph/service/AliyunTokenService.java:24`）。

长语音异步：

- `upload-long-audio` 提交任务后返回 `taskId`，再用 `/api/notes/filetrans-status` 轮询（`src/main/java/com/study/kgraph/controller/NoteController.java:233`）。

老师追问“为什么要异步”：

- 大音频识别时间长，同步会阻塞请求、容易超时；异步 + 轮询更稳。

### 5.4 上传文档资料并抽取文本

- 上传文档变笔记：`/api/notes/upload-doc`（`src/main/java/com/study/kgraph/controller/NoteController.java:70`）
  - 先保存文件到本地目录，再用 Tika 抽取文本（同文件 `:79-90`）。

- 上传“资料素材”：`/api/material/upload`（`src/main/java/com/study/kgraph/controller/MaterialController.java:24`）
  - 同样是保存文件 + Tika 抽取内容，再存入 `materials` 表（同文件 `:31-44`）。

### 5.5 知识图谱怎么生成（最核心亮点）

1) 前端请求：`graph.js` 会请求 `api/graph/build`（`src/main/resources/static/js/graph.js:3`）。

2) 后端聚合文本：`GraphController.build` 拉取当前用户的 notes + materials，把文本拼起来（`src/main/java/com/study/kgraph/controller/GraphController.java:30-45`）。

3) NLP 抽取关键词：`GraphService.extractGraph` 调用 `nlpService.keywords(text, 30)` 得到关键词列表（`src/main/java/com/study/kgraph/service/GraphService.java:20-23`）。

4) 计算节点权重：用词频作为 `score`（同文件 `:27-30`），前端用 `score` 控制节点半径（`src/main/resources/static/js/graph.js:66-68`）。

5) 计算边（关系）：同一句出现则记一次共现，出现次数越多权重越大（`src/main/java/com/study/kgraph/service/NLPService.java:19-31`，`src/main/java/com/study/kgraph/service/GraphService.java:32-49`）。

6) D3 可视化：力导向图 + 拖拽 + 缩放（`src/main/resources/static/js/graph.js:35-40`、`:27-29`、`:179-200`）。

老师追问“为什么用共现做关系”：

- 共现是一个简单但有效的启发式规则，不依赖复杂训练，适合课程项目。

### 5.6 AI 总结功能（可作为亮点回答）

- `/api/notes/summarize`：将笔记内容发给模型做总结，并写回 `notes.summary`（`src/main/java/com/study/kgraph/controller/NoteController.java:361-381`）。
- `AiSummaryService.summarize` 通过 HTTP 调用大模型接口（`src/main/java/com/study/kgraph/service/AiSummaryService.java:29`）。

---

## 6. 数据库表设计（按老师问法解释）

初始化脚本在 `src/main/resources/schema.sql`。

### 6.1 用户表 `users`

- 作用：登录注册。
- 字段：`username`、`password_hash`、`created_at`（`schema.sql:1-6`）。

### 6.2 笔记表 `notes`

- 作用：保存从语音/文档生成或手工编辑的笔记。
- 字段：`title`、`text`、`summary`、`audio_path`、时间戳（`schema.sql:7-16`）。

### 6.3 资料表 `materials`

- 作用：保存上传的资料与抽取文本。
- 字段：`type`、`title`、`file_path`、`extracted_text`（`schema.sql:17-25`）。

### 6.4 知识图谱结构表 `concepts` / `relations`

- `concepts`：节点（概念词），`score` 是权重（`schema.sql:26-31`）。
- `relations`：边（概念之间关系），`relation_type` 默认为 `cooccur`，`weight` 是边权重（`schema.sql:32-39`）。

当前实现说明：图谱接口主要“实时从文本抽取并返回前端”，表结构为后续“持久化图谱”留接口。

### 6.5 长音频任务表 `note_tasks`

- 作用：记录长语音识别任务的 `task_id` 与状态，便于轮询更新（`schema.sql:40-48`）。

### 6.6 版本表 `note_versions`

- 作用：记录笔记历史版本（方便追溯/回滚）。
- 插入逻辑：`version_number = max(version_number)+1`（`src/main/resources/mapper/NoteVersionMapper.xml:16-23`）。
- 查询最近 50 条历史：`findMetaByNoteId`（同文件 `:26-32`）。

---

## 7. 关键配置与运行方式（macOS）

### 7.1 数据库配置

配置文件：`src/main/resources/application.properties`。

- `jdbc.url` 指向本地 MySQL 的 `kgraph` 数据库（`application.properties:1`）。
- 用户名/密码不要写进汇报材料，汇报时说“本机 MySQL 账号密码已在配置文件中设置”。

### 7.2 上传目录

- `upload.root` 决定文件保存到哪里（`application.properties:8`）。

### 7.3 阿里云 / 大模型配置（敏感信息）

项目支持 `.env` 方式加载密钥：

- 读取 `.env` 并写入 `System.setProperty`：`EnvInitListener`（`src/main/java/com/study/kgraph/config/EnvInitListener.java:16-33`）。
- 模板示例：`.env.example`。

汇报时的说法：

- “云服务密钥属于敏感信息，实际运行时放在本地 `.env`，不公开。”

### 7.4 编译与运行

项目是 Maven `war` 项目，可以二选一：

1) 用 Tomcat 部署 `war`（更传统）

- `mvn clean package -DskipTests`
- 将生成的 `target/*.war` 部署到 Tomcat 的 `webapps/`。

2) 使用 Maven Tomcat 插件（更方便演示）

- `mvn tomcat7:run`
- 访问 `http://localhost:8080/`。

证据：`pom.xml:145-154` 配置了 tomcat7-maven-plugin。

---

## 8. 老师高频提问与标准回答（背下来就够用）

### Q1：这个项目核心创新点是什么？

A：把“语音/文档”自动转成“可编辑笔记”，再用 NLP 抽取关键词并构建知识图谱，通过可视化把知识点之间的关系展示出来，提高复习效率。

### Q2：知识图谱的节点和边怎么来的？

A：节点来自 HanLP 的关键词抽取（`NLPService.keywords`），边来自关键词在同一句中的共现次数（`NLPService.cooccurrence`），次数越多权重越大。

### Q3：为什么不直接把图谱永久存数据库？

A：当前版本为了演示“从用户内容实时生成图谱”，因此接口是实时抽取后返回；数据库已设计好 `concepts/relations` 表，后续可以把结果持久化，支持多次复用和历史对比。

### Q4：语音识别为什么要分短语音/长语音？

A：短语音可以同步返回，体验快；长语音识别耗时长，必须异步提交任务并轮询状态，避免请求超时。

### Q5：登录是怎么做的？有没有权限控制？

A：登录成功后把 `userId` 放入 Session。拦截器 `LoginInterceptor` 会在请求前检查 Session 是否存在 `userId`，没有则对 API 返回 401，对页面重定向到登录页。

### Q6：如果老师问“安全性怎么样”？

A：课程项目优先完成业务闭环；目前密码使用 MD5（便于演示），如果要更安全可升级为加盐哈希（如 BCrypt）并对密钥配置做环境隔离（本项目已支持 `.env`）。

---

## 9. 演示顺序（1 分钟讲清楚）

1) 注册/登录
2) 上传一段音频 → 自动转写生成笔记
3) 上传一个 PDF/Word → 自动抽取文本生成笔记/素材
4) 进入知识图谱 → 展示关键词节点大小与关系边
5) 打开笔记列表 → 编辑/总结（如果配置了大模型接口）

---

## 10. 项目目录结构（老师问“代码在哪”时）

- Controller：`src/main/java/com/study/kgraph/controller/`
- Service：`src/main/java/com/study/kgraph/service/`
- Mapper 接口：`src/main/java/com/study/kgraph/mapper/`
- Mapper XML：`src/main/resources/mapper/`
- 页面模板：`src/main/resources/templates/`
- 静态资源：`src/main/resources/static/`
- 数据库脚本：`src/main/resources/schema.sql`

---

## 11. 可扩展方向（老师问“还能做什么”）

- 图谱持久化与增量更新：把抽取结果存入 `concepts/relations`，支持历史版本对比。
- 知识点路径分析：输入两个概念，输出关联路径。
- 问答检索（RAG）：从笔记/资料检索相关段落再让模型回答，提高准确率。
- 复习计划：根据更新频率和遗忘曲线生成复习任务。

---

## 12. 一句话收尾

这个项目的价值在于把“原始学习材料 → 结构化笔记 → 知识图谱可视化”串成闭环，用更直观的方式帮助学习与复习。

